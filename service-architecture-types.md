# 서비스 아키텍처 유형 분류 (내 나침반)

> 목적: "이 기능은 어떤 유형이고, 뭐가 필요하고, 언제 다음 단계로 갈지"를 빠르게 판단하려고.
> 핵심 통찰: **유형은 하나의 잣대가 아니라 여러 축의 조합**이다. 실제 서비스는 여러 유형을 **섞어** 쓴다.

## 1. 분류 축 (기준은 여러 개다)

| 축 | 한쪽 끝 → 반대쪽 끝 |
|---|---|
| **콘텐츠** | 정적(고정 HTML) → 동적(JS가 그리고 반응) |
| **데이터 갱신** | 없음 → 주기적(배치) → 읽을 때(요청 시점) → 사용자가 씀(생성/수정) |
| **상태 저장 위치** | 없음 → 브라우저(localStorage) → 서버·DB |
| **인증** | 불필요 → 필요(개인화·쓰기 보호) |
| **서버 로직 무게** | 없음(정적 호스팅) → 얇은 함수(서버리스) → 상시 서버(+DB) |

한 서비스의 "유형"은 이 축들의 **좌표 조합**이다. 아래 사다리는 자주 나오는 조합을 이름 붙인 것.

## 2. 대표 유형 사다리 (조합에 이름 붙이기)

| 층 | 유형 | 한 줄 | 필요한 인프라 | 예시 |
|---|---|---|---|---|
| **L0** | 순수 정적 공유 | 고정 콘텐츠, 갱신 없음 | 정적 호스팅 (GitHub Pages/S3) | 문서, 랜딩, 이력서 |
| **L1** | 정적 + 클라 인터랙션 | 페이지는 반응하지만 서버 데이터 없음 | 정적 호스팅 | 계산기, 클라 게임, 미제출 폼 |
| **L2** | 주기 갱신 정적 | 데이터가 배치로 갱신(사용자와 무관) | 정적 호스팅 + 스케줄러(cron/Actions) | 뉴스 요약, **youtube-feed 추천채널** |
| **L3** | 요청시 동적 (무상태) | 볼 때 함수가 데이터 가져옴, 저장은 안 함 | 정적 + **서버리스 함수** | 검색 프록시, **곧 만들 Worker** |
| **L4** | 클라 개인화 | 사용자별 상태를 브라우저에 저장 | 정적 호스팅 | **youtube-feed 내 채널(localStorage)** |
| **L5** | 계정 + 저장 | 인증 + 서버 DB에 사용자/공유 데이터 쓰기 | 서버리스/BaaS + DB + Auth | 로그인 앱, 댓글, **여러 기기 동기화(Supabase)** |
| **L6** | 풀 백엔드 | 복잡 로직·실시간·상시 처리 | 상시 서버(+DB·큐 등) | 채팅, 게임서버, 대규모 API |

> 위로 갈수록 **강력하지만 관리·비용이 커진다.** L2~L4는 "서버 없음/거의 없음"이라 무료로 오래 버틴다.
> L5부터 "진짜 백엔드"의 세계(DB·인증·보안)로 넘어간다.

## 3. youtube-feed는 지금 어디? (섞여 있다는 증거)

- **추천 채널 = L2** (Actions가 3시간마다 data.json 갱신, 사용자 무관)
- **내 채널 = L4** (localStorage에 사용자별 목록) — 이미 **L2 + L4 하이브리드**
- **지금 하려는 것**: 내 채널 데이터를 남의 공짜 프록시 대신 **내 Cloudflare Worker**로 → **L3 능력 추가** (요청시 동적, 무상태)
- **미래**: "폰에서 추가한 채널을 노트북에서도" = 기기 간 동기화 → 인증+DB 필요 → **L5** (그때 Supabase)

## 4. 판단 원칙 (신호등)

- 유형은 **여러 축의 조합**이고, 좋은 서비스는 필요한 층만 **골라 섞는다** (전부 L6일 필요 없음).
- 위 층은 **아플 때만** 올라간다. 안 아프면 아래층이 이득(싸고 단순하고 안 터짐).
- 지금 신호등: 공짜 프록시가 자주 실패(L3를 남에게 의존) → **내 Worker로 L3를 내재화**할 때.
- 다음 신호등: "한 기기 한정(localStorage)이 답답, 여러 기기 동기화 필요" → **L5(인증+DB)**로.
