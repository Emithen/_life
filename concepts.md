# 새로 알게 된 개념 노트

> 목적: 작업하다 처음 만난 개념을 **짧게** 붙잡아 둔다.
> 교과서가 아니라 **"나중에 이 이름이 떠오르게 하는 것"**이 목적이다.
>
> 규칙 세 개:
> 1. **한 항목 = 화면 반 쪽.** 길어지면 그건 개념이 아니라 문서다 → `docs/core/` 로 보낸다.
> 2. **어디서 실제로 썼는지 반드시 링크.** 안 쓴 개념은 안 적는다(적어도 안 남는다).
> 3. **헷갈렸던 지점을 적는다.** 정의는 검색하면 나오지만, 내가 왜 헷갈렸는지는 안 나온다.
>
> 관련: 아키텍처 유형 판단은 `service-architecture-types.md`, 프로젝트별 구조는 `docs/core/`.

---

## CSS `@layer` (캐스케이드 레이어) — 2026-08-05

**한 줄**: CSS 우선순위를 셀렉터 특정도가 아니라 **"레이어 순서"로 정하는** 표준 기능.

```css
/* 이 한 줄이 우선순위를 정한다. 뒤에 올수록 강하다. */
@layer reset, tokens, base, components, screens;

@layer components { .btn { color: blue; } }
@layer screens    { .btn { color: red;  } }   /* 이긴다 — 뒤 레이어라서 */
```

- 레이어 **사이**는 순서가 이긴다. 특정도는 **레이어 안에서만** 평소처럼 작동한다.
- 그래서 `#id`로 쓴 규칙도 뒷 레이어의 `.class` 하나에 진다.

**왜 좋은가 (내가 이걸 도입한 이유)**
화면별 규칙이 공통 컴포넌트 규칙을 덮어야 할 때, 보통은 셀렉터를 길게 쓰거나
`!important`를 붙여 **"이기려고 싸운다."** 레이어를 쓰면 그 싸움 자체가 없어진다 —
`screens`가 `components`보다 뒤라는 것만으로 끝난다.

**⚠️ 제일 헷갈렸던 것 — 레이어 밖 규칙이 모든 레이어보다 강하다**
직관과 반대다. "레이어에 안 넣었으니 약하겠지"가 아니라 **가장 세다.**
실제로 확인해봤다(`@layer a,b` 선언 후 a=1, b=2, 레이어 밖=3 → **3이 이김**).

→ 실무적 함의: 레이어를 도입했으면 **모든 규칙을 레이어 안에 넣어야** 한다.
실수로 하나를 밖에 두면 그게 전부를 이겨서 "왜 안 덮이지?"가 된다.
(`!important`는 반대로 뒤집힌다 — 앞 레이어의 `!important`가 더 세다. 그래서 더 안 쓰게 된다.)

**어디에 썼나**: `youtube-feed/src/style.css` — `reset / tokens / base / components / screens`
5개로 구획. `screens`는 화면 분리(갈래1 2.5단계)를 위해 **미리 비워둔** 레이어다.

**왜 파일 분리 대신 이걸 골랐나**: 빌드 단계가 없어서 CSS 파일을 늘리면
`?v=` 캐시 버스팅을 파일 수만큼 손으로 관리해야 한다(이미 여섯 곳).
레이어는 **파일을 안 늘리고 계층만** 준다.

**지원**: 2022년 이후 브라우저 전부. 폴리필 불필요.

**더 볼 키워드**: cascade layers · specificity · `:where()`(특정도를 0으로 만드는 함수) ·
`@scope` · CSS Modules / Tailwind가 이 문제를 각각 어떻게 푸는지
